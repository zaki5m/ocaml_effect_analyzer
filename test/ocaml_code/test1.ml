(* 直線的な関数呼び出しのみ *)
let add a b = 
  a + b

let sub a b =
  a - b

let mul a b =
  a * b

let main = 
  let a = 1 in
  let b = 2 in
  let _ = add a b in
  let _ = sub a b in
  let _ = mul a b in
  ()
  